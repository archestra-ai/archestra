"use client";

/** Scaffold placeholder; WP2 replaces the body. */
export function OverviewTab() {
  return (
    <div className="rounded-lg border p-4 text-sm text-muted-foreground">
      <span>The overview is not built yet.</span>
    </div>
  );
}
