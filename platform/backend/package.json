{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "start": "node/main.js start",
    "dev": "nodemon --exec \"tsc && node dist/main.js\" --watch src --ext ts",
    "lint": "biome check --write",
    "format": "biome format --write",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "nodemon": "^3.1.10",
    "pino-pretty": "^13.1.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.0"
  }
}
