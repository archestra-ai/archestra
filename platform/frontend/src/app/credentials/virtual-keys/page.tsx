// Virtual keys are an LLM-proxy credential concept; the management UI is shared.
// Rendered here under the unified Credentials layout, whose action-button
// context is the shared CredentialsActionContext the LLM page reads.
export { default } from "@/app/llm/credentials/virtual-keys/page";
