id = "finance-eval"
name = "Finance & data analysis"
tasks = ["lbo-model", "earnings-extract", "comps-capm", "ib-deck-qc", "sql-analytics"]

# Pure-compute tasks over staged inputs: the agent reads the attachments, computes in the sandbox, and
# hands the answer to submit_result. No skills or MCPs are seeded (none are needed), and the tasks are
# read-only against backend state, so all lanes can share one seeded backend.
share_backend = true
