# Live qualification policy for the OpenAPPA root tool flow.
#
# Install it as the organization guardrails policy and run the scenarios as
# described in this directory's README.md. It carries no credentials.
#
# One policy for three clients. Claude Code tools use `host/claude-code/*`.
# Codex and OpenCode tools use bare names or `builtin:`. The Archestra runtime
# converts them to `host/archestra/<name>`. Each Codex and OpenCode tool is listed
# under `host/archestra/*` and as `builtin:*` for backward compatibility.
#
# The scenario files below are absolute paths, seeded by `setup-lab.sh`. They
# name the default lab directory, `${APPA_LAB_HOME:-/tmp/appa-root-flow}/lab`;
# a lab somewhere else needs these paths rewritten before the policy is
# installed (setup-lab.sh prints the one-liner when it detects this).
#
# Scenarios, each reachable from a single prompt:
#
#   S1 accept    reading /tmp/appa-root-flow/lab/secret.txt restricts the
#                session's audience, so the read is blocked with an acceptance
#                plan.
#   S1c proof    the same call answers differently before and after S1, which
#                is what shows the acceptance narrowed the session rather than
#                merely completing a round trip. Chat reaches it through
#                `archestra__load_skill`; see README.md.
#   S2 input     sending that content out needs the `redactor` input sanitizer,
#                which rewrites the arguments and releases the call.
#   S3 output    reading /tmp/appa-root-flow/lab/report.txt stages its output
#                behind the `summarize` output sanitizer, which returns the
#                admitted value.
#   S3c return   Reading /tmp/appa-root-flow/lab/return.txt restricts only the
#                child trajectory. Its tool result remains in the child.
#                The summarize sanitizer governs the final ChildEnd return
#                into the parent session.
#   S4 authority publishing needs the `operator` authority, a URL fixture this
#                run controls (approve / deny / timeout / no answer).
#   S5 planless  a tool the policy does not name is blocked with no plan.

[policy]
version = 2
trust_chain = ["untrusted", "trusted"]

# --- the session's ordinary tools -------------------------------------------

[[policy.tool]]
name = "host/claude-code/Glob"
delta = {}

[[policy.tool]]
name = "host/claude-code/Grep"
delta = {}

[[policy.tool]]
name = "host/claude-code/TodoWrite"
delta = {}

[[policy.tool]]
name = "host/claude-code/Task"
delta = {}

[[policy.tool]]
name = "host/claude-code/Agent"
delta = {}

[[policy.tool]]
name = "host/claude-code/Skill"
delta = {}

[[policy.tool]]
name = "builtin:list"
delta = {}

[[policy.tool]]
name = "builtin:glob"
delta = {}

[[policy.tool]]
name = "builtin:grep"
delta = {}

[[policy.tool]]
name = "builtin:todowrite"
delta = {}

[[policy.tool]]
name = "builtin:spawn_agent"
delta = {}

[[policy.tool]]
name = "builtin:send_input"
delta = {}

[[policy.tool]]
name = "builtin:wait_agent"
delta = {}

[[policy.tool]]
name = "builtin:resume_agent"
delta = {}

[[policy.tool]]
name = "builtin:close_agent"
delta = {}

[[policy.tool]]
name = "builtin:interrupt_agent"
delta = {}

[[policy.tool]]
name = "builtin:task"
delta = {}

[[policy.tool]]
name = "builtin:skill"
delta = {}

# --- S1: a read that restricts the session ----------------------------------

[[policy.tool]]
name = "host/claude-code/Read(file_path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:read(filePath:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:Read(file_path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:Read(path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:shell(command:*secret.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:exec_command(cmd:*secret.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/claude-code/Read(file_path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:read(filePath:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:Read(file_path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:Read(path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:shell(command:*secret-other.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "builtin:exec_command(cmd:*secret-other.txt*)"
delta = { audience = ["lab@archestra.local"] }

# --- S3: a read whose output is staged behind a sanitizer -------------------

[[policy.tool]]
name = "host/claude-code/Read(file_path:/tmp/appa-root-flow/lab/report.txt)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "builtin:read(filePath:/tmp/appa-root-flow/lab/report.txt)"
delta = { trust = "untrusted" }

# Codex has no read tool of its own; it reads a file with a shell command.
[[policy.tool]]
name = "builtin:exec_command(cmd:*report.txt*)"
delta = { trust = "untrusted" }

# --- S3c: child-only data, sanitized only at the return boundary ------------

[[policy.tool]]
name = "host/claude-code/Bash(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "builtin:shell(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "builtin:bash(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "builtin:exec_command(cmd:*return.txt*)"
delta = { trust = "untrusted" }

# --- ordinary reads ----------------------------------------------------------

[[policy.tool]]
name = "host/claude-code/Read"
delta = {}

[[policy.tool]]
name = "builtin:read"
delta = {}

[[policy.tool]]
name = "builtin:Read"
delta = {}

# --- S2: the sink that carries content out ----------------------------------

[[policy.tool]]
name = "host/claude-code/Write"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "builtin:write"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "builtin:apply_patch"
requires = { audience = { contains = ["public"] } }
delta = {}

# The child-lineage smoke test writes to this destination. This rule covers
# shell tools and write tools. The model cannot bypass the governed sink by
# choosing a different tool.
[[policy.tool]]
name = "host/claude-code/Bash(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "builtin:shell(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "builtin:bash(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "builtin:exec_command(cmd:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

# --- S4: the sink a person must clear ---------------------------------------

[[policy.tool]]
name = "host/claude-code/Bash(command:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "builtin:shell(command:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "builtin:bash(command:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "builtin:exec_command(cmd:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

# --- ordinary shells ---------------------------------------------------------

[[policy.tool]]
name = "host/claude-code/Bash"
delta = {}

[[policy.tool]]
name = "builtin:shell"
delta = {}

[[policy.tool]]
name = "builtin:bash"
delta = {}

# Codex's own tools, under the names the proxy submits for it.
[[policy.tool]]
name = "builtin:exec_command"
delta = {}

[[policy.tool]]
name = "builtin:write_stdin"
delta = {}

[[policy.tool]]
name = "builtin:view_image"
delta = {}

[[policy.tool]]
name = "builtin:request_user_input"
delta = {}

[[policy.tool]]
name = "builtin:list_mcp_resources"
delta = {}

[[policy.tool]]
name = "builtin:list_mcp_resource_templates"
delta = {}

[[policy.tool]]
name = "builtin:read_mcp_resource"
delta = {}

[[policy.tool]]
name = "builtin:get_goal"
delta = {}

[[policy.tool]]
name = "builtin:create_goal"
delta = {}

[[policy.tool]]
name = "builtin:update_goal"
delta = {}

# Codex writes files through a free-form custom tool; the proxy carries its one
# argument as `input`, so the policy names it like any other sink.
[[policy.tool]]
name = "builtin:apply_patch"
requires = { audience = { contains = ["public"] } }
delta = {}

# --- Codex / OpenCode as evaluated by the runtime ----------------------------
#
# Bare names and `builtin:` derive to `host/archestra/<name>`. A policy that
# lists only `builtin:*` refuses `spawn_agent` and `exec_command` as undeclared tools.

[[policy.tool]]
name = "host/archestra/Glob"
delta = {}

[[policy.tool]]
name = "host/archestra/glob"
delta = {}

[[policy.tool]]
name = "host/archestra/Grep"
delta = {}

[[policy.tool]]
name = "host/archestra/grep"
delta = {}

[[policy.tool]]
name = "host/archestra/list"
delta = {}

[[policy.tool]]
name = "host/archestra/todowrite"
delta = {}

[[policy.tool]]
name = "host/archestra/spawn_agent"
delta = {}

# Claude Code local tools use bare names. The Archestra runtime derives
# them under `host/archestra/<name>` and preserves capitalization.
[[policy.tool]]
name = "host/archestra/Agent"
delta = {}

[[policy.tool]]
name = "host/archestra/Task"
delta = {}

[[policy.tool]]
name = "host/archestra/Skill"
delta = {}

[[policy.tool]]
name = "host/archestra/SendMessage"
delta = {}

[[policy.tool]]
name = "host/archestra/ListAgents"
delta = {}

[[policy.tool]]
name = "host/archestra/TaskStop"
delta = {}

[[policy.tool]]
name = "host/archestra/send_input"
delta = {}

[[policy.tool]]
name = "host/archestra/wait_agent"
delta = {}

[[policy.tool]]
name = "host/archestra/resume_agent"
delta = {}

[[policy.tool]]
name = "host/archestra/close_agent"
delta = {}

[[policy.tool]]
name = "host/archestra/interrupt_agent"
delta = {}

[[policy.tool]]
name = "host/archestra/task"
delta = {}

[[policy.tool]]
name = "host/archestra/skill"
delta = {}

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*secret.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/shell(command:*secret.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/read(filePath:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/read(path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/Read(file_path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/Read(path:/tmp/appa-root-flow/lab/secret.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*secret-other.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/shell(command:*secret-other.txt*)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/read(filePath:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/read(path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/Read(file_path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/Read(path:/tmp/appa-root-flow/lab/secret-other.txt)"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*report.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/shell(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/bash(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/Bash(command:*return.txt*)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/read(filePath:/tmp/appa-root-flow/lab/report.txt)"
delta = { trust = "untrusted" }

[[policy.tool]]
name = "host/archestra/read"
delta = {}

[[policy.tool]]
name = "host/archestra/Read"
delta = {}

[[policy.tool]]
name = "host/archestra/write"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/Write"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/apply_patch"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/shell(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/bash(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/Bash(command:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*out-child.txt*)"
requires = { audience = { contains = ["public"] } }
delta = {}

[[policy.tool]]
name = "host/archestra/exec_command(cmd:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "host/archestra/shell(command:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "host/archestra/bash(command:*appa-publish*)"
requires = { attention = ["operator-signoff"] }
delta = {}

[[policy.tool]]
name = "host/archestra/shell"
delta = {}

[[policy.tool]]
name = "host/archestra/bash"
delta = {}

[[policy.tool]]
name = "host/archestra/Bash"
delta = {}

[[policy.tool]]
name = "host/archestra/exec_command"
delta = {}

[[policy.tool]]
name = "host/archestra/write_stdin"
delta = {}

[[policy.tool]]
name = "host/archestra/view_image"
delta = {}

[[policy.tool]]
name = "host/archestra/request_user_input"
delta = {}

[[policy.tool]]
name = "host/archestra/list_mcp_resources"
delta = {}

[[policy.tool]]
name = "host/archestra/list_mcp_resource_templates"
delta = {}

[[policy.tool]]
name = "host/archestra/read_mcp_resource"
delta = {}

[[policy.tool]]
name = "host/archestra/get_goal"
delta = {}

[[policy.tool]]
name = "host/archestra/create_goal"
delta = {}

[[policy.tool]]
name = "host/archestra/update_goal"
delta = {}

# --- Archestra Chat, under the gateway tool names the proxy submits for it ---
#
# Chat has no filesystem tools of its own, so it reaches S1 through a gateway
# tool instead: listing the skills restricts the session's audience, so the call
# is blocked with the same acceptance plan the other clients get for reading the
# secret. Chat needs no entry for the APPA tools themselves — the gateway
# advertises them to every conversation while OpenAPPA is enabled.

[[policy.tool]]
name = "archestra__list_skills"
delta = { audience = ["lab@archestra.local"] }

[[policy.tool]]
name = "archestra__search_tools"
delta = {}

[[policy.tool]]
# S1c. Chat's proof that the acceptance actually changed the session, not just
# that the round trip happened: loading a skill is fine while the session is
# public, and the same call stops meeting this requirement once list_skills
# has narrowed the audience. The `redactor` input sanitizer below is the
# remedy offered for it, exactly as it is for the clients' own write tools.
name = "archestra__load_skill"
requires = { audience = { contains = ["public"] } }
delta = {}

# --- the remedies -----------------------------------------------------------

[[policy.sanitizer]]
name = "redactor"
on = ["tool_input"]

[policy.sanitizer.permits]
audience = { from = ["lab@archestra.local"], to = ["public"] }

[[policy.sanitizer]]
name = "summarize"
on = ["tool_output"]

[policy.sanitizer.permits]
trust = { from = "untrusted", to = "trusted" }

# Result points for output sanitizers: Reading report.txt stages output until
# the session accepts the summarized value. Shell results stay unconfined so
# return.txt remains visible in the child. The sanitizer applies only when
# ChildEnd returns the answer to the parent.
[policy.deployment]
context_control = true
confined_results = ["host/claude-code/Read", "builtin:read", "host/archestra/read", "host/archestra/Read"]

[[policy.authority]]
name = "operator"
hint = "The operator on call. Consult them before anything is published."

[policy.authority.permits]
attention = ["operator-signoff"]

[externals]
timeout_ms = 5000
review_timeout_ms = 20000
max_body_bytes = 65536

# The fixture this run controls (`externals-fixture.mjs`): it answers as
# approve, deny, timeout or no answer, which is the only boundary this
# qualification mocks. Change the port here if you start it on another one.
[externals.authorities.operator]
url = "http://127.0.0.1:8899/authority"

[externals.sanitizers.redactor]
url = "http://127.0.0.1:8899/sanitizer/redact"

[externals.sanitizers.summarize]
url = "http://127.0.0.1:8899/sanitizer/summarize"
