"use client";

import { BatteriesPanel } from "./batteries-panel";

/** Scaffold placeholder that shows today's panel; WP5 replaces the body and deletes the panel. */
export function BatteriesTab() {
  return <BatteriesPanel />;
}
