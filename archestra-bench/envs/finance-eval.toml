id = "finance-eval"
name = "Finance deck QC & LBO modeling"
tasks = ["lbo-model", "ib-deck-qc"]

# Finance judgement tasks over staged inputs: the agent reads the attachments, works in the sandbox,
# and hands the answer to submit_result. No skills or MCPs are seeded (none are needed), and the tasks
# are read-only against backend state, so all lanes can share one seeded backend.
share_backend = true
