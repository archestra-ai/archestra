# GENERATED — do not edit by hand.
#
# Slim Dagger Engine for the quickstart image: the same companion chart used in
# production (helm/dagger-runtime) and dev (dev/Tiltfile.coderuntime), rendered
# offline with laptop-sized resources so the embedded KinD cluster can schedule
# it. The backend reaches it over kube-pod:// (kubectl exec + buildctl
# dial-stdio), so no Service or TCP listener is rendered.
#
# Regenerate after bumping helm/dagger-runtime:
#   helm template dagger-runtime ./helm/dagger-runtime --namespace default \
#     --set networkPolicy.create=false \
#     --set dagger.engine.resources.requests.cpu=500m \
#     --set dagger.engine.resources.requests.memory=2Gi \
#     --set dagger.engine.resources.limits.memory=6Gi \
#     --set dagger.engine.statefulSet.persistentVolumeClaim.resources.requests.storage=20Gi \
#     > docker/k8s/dagger-engine.quickstart.yaml
#
# pod: dagger-runtime-engine-0  container: dagger-engine  namespace: default
---
# Source: archestra-dagger-runtime/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dagger-runtime
  namespace: default
  labels:
    helm.sh/chart: archestra-dagger-runtime-0.1.0
    app.kubernetes.io/name: archestra-dagger-runtime
    app.kubernetes.io/instance: dagger-runtime
    app.kubernetes.io/version: "0.21.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: archestra
automountServiceAccountToken: false

---
# Source: archestra-dagger-runtime/charts/dagger/templates/engine-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: dagger-runtime-engine-config
  namespace: default
  labels:
    helm.sh/chart: dagger-0.21.0
    app.kubernetes.io/name: dagger
    app.kubernetes.io/instance: dagger-runtime
    app.kubernetes.io/version: v0.21.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: dagger
data:
  engine.json: |
    {
      "logLevel": "info",
      "security": {
        "insecureRootCapabilities": false
      },
      "gc": {
        "maxUsedSpace": "40GB",
        "reservedSpace": "5GB",
        "minFreeSpace": "20%"
      }
    }

---
# Source: archestra-dagger-runtime/charts/dagger/templates/engine-statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dagger-runtime-engine
  namespace: default
  labels:
    helm.sh/chart: dagger-0.21.0
    app.kubernetes.io/name: dagger
    app.kubernetes.io/instance: dagger-runtime
    app.kubernetes.io/version: v0.21.0
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: dagger
spec:
  # DO NOT RUN MORE THAN 1 REPLICA IF USING HOSTPATH.
  # Only one (1) Engine is able to hold the lock, other replicas will not be able to start.
  # If using persistent volumes, this is not an issue. Use more than 1 replica if you know that you need it.
  replicas: 1
  podManagementPolicy: OrderedReady
  selector:
    matchLabels:
      name: dagger-runtime-engine
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  template:
    metadata:
      annotations:
        checksum/config: 573d12e5d7633e692f3021e22d8552d6d6c47f6782c1193d4ebc1aab18d37cbc
      labels:
        name: dagger-runtime-engine
        helm.sh/chart: dagger-0.21.0
        app.kubernetes.io/name: dagger
        app.kubernetes.io/instance: dagger-runtime
        app.kubernetes.io/version: v0.21.0
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/part-of: dagger
    spec:
      securityContext:
        runAsUser: 0
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: "OnRootMismatch"
      serviceAccountName: dagger-runtime
      containers:
        - name: dagger-engine
          image: registry.dagger.io/engine:v0.21.0
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            capabilities:
              add:
                - ALL
          resources:
            limits:
              memory: 6Gi
            requests:
              cpu: 500m
              memory: 2Gi
          lifecycle:
          readinessProbe:
            exec:
              command:
              - sh
              - -exc
              - dagger core version
            failureThreshold: 10
            initialDelaySeconds: 5
            periodSeconds: 15
            successThreshold: 1
            timeoutSeconds: 14
          livenessProbe:
            exec:
              command:
              - sh
              - -exc
              - dagger core version
            failureThreshold: 20
            initialDelaySeconds: 5
            periodSeconds: 30
            successThreshold: 1
            terminationGracePeriodSeconds: 30
            timeoutSeconds: 29
          volumeMounts:
            - name: data
              mountPath: /var/lib/dagger
            - name: config
              mountPath: /etc/dagger/engine.json
              subPath: engine.json
            - mountPath: /run/dagger
              name: run
      terminationGracePeriodSeconds: 300
      volumes:
        - name: config
          configMap:
            name: dagger-runtime-engine-config
            items:
              - key: engine.json
                path: engine.json
        - emptyDir:
            medium: Memory
          name: run
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 20Gi
