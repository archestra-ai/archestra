import type { FastifyPluginAsyncZod } from "fastify-type-provider-zod";

const userTokenRoutes: FastifyPluginAsyncZod = async (fastify) => {
  // TODO: Implement user token routes
};

export default userTokenRoutes;
