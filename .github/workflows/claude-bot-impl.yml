# Reusable workflow for bot-initiated Claude actions.
# Wraps the common pattern: GitHub App token + checkout + claude-code-action with API key auth.
# Called by claude-pr-triage.yml and claude-issue-triage.yml.
#
# Comments appear as "Archestra Triage Sheriff" bot via the GitHub App token.
name: Claude Bot Action (Implementation)

on:
  workflow_call:
    inputs:
      prompt:
        description: The prompt to send to Claude
        required: true
        type: string
      allowed_tools:
        description: Comma-separated list of allowed Claude tools
        required: true
        type: string
      timeout_minutes:
        description: Job timeout in minutes
        required: false
        type: number
        default: 10
      use_sticky_comment:
        description: Use sticky comments to reduce notification noise
        required: false
        type: boolean
        default: false
      track_progress:
        description: Track progress in PR/issue comments
        required: false
        type: boolean
        default: false
    secrets:
      ANTHROPIC_API_KEY:
        required: true
      TRIAGE_SHERIFF_APP_ID:
        required: true
      TRIAGE_SHERIFF_PRIVATE_KEY:
        required: true

jobs:
  run-claude:
    runs-on: ubuntu-latest
    timeout-minutes: ${{ inputs.timeout_minutes }}
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@df432ceedc7162793a195dd1713ff69aefc7379e # v2.0.6
        with:
          app-id: ${{ secrets.TRIAGE_SHERIFF_APP_ID }}
          private-key: ${{ secrets.TRIAGE_SHERIFF_PRIVATE_KEY }}

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Run Claude
        uses: anthropics/claude-code-action@1b8ee3b94104046d71fde52ec3557651ad8c0d71 # v1.0.29
        with:
          prompt: ${{ inputs.prompt }}
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          github_token: ${{ steps.app-token.outputs.token }}
          allowed_non_write_users: "*"
          use_sticky_comment: ${{ inputs.use_sticky_comment }}
          track_progress: ${{ inputs.track_progress }}
          claude_args: |
            --allowedTools "${{ inputs.allowed_tools }}"
